package name.sample.graphs;

/**
 * An unweighted is a simple {@link Graph} structure in which each edge does not have an associated "weight" object.
 *
 * @param <V> graph vertex type
 */
public interface UnweightedGraph<V> extends Graph<V, Edge<V>> {
}
